services:
  teams-mcp:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../.env
    ports:
      - "9106:9106"
    volumes:
      - teams_mcp_data:/app/data
    restart: unless-stopped

volumes:
  teams_mcp_data:
