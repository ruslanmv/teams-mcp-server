"""ACS (Azure Communication Services) package for real-time meeting audio.

Provides tools for joining a Teams meeting as a bot participant via ACS
Teams interop, capturing the audio stream, and piping it to the STT pipeline.

Requires: ``azure-communication-calling`` (optional dependency).
"""
